"""Shared helper package for Step 5 AI tests.

``ai_fixtures`` is imported as a stable package name from both the
``tests/features`` and ``tests/ai`` test packages so the helper module has a
single import identity under pytest's flat test layout.
"""
